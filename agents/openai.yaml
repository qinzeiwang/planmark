interface:
  display_name: "PlanMark"
  short_description: "Site-map redraw and annotation / 底图重绘与设备标注"
  default_prompt: "Use $planmark to redraw this site image, ask me to confirm the base map, then open the annotation editor. 使用 $planmark 重绘底图，确认后进入标注。"
